// function loafifyPage(request) {
//   console.log("loafified page!")
//   var acronyms = Object.entries(request.loafify)
//   console.log(acronyms)

//   acronyms.forEach(([acronym, fullText]) => {
//     // document.querySelectorAll("p").forEach(node => {
//     //   node.innerHTML = node.innerHTML.replaceAll(acronym, `<abbr title=${fullText}>${acronym}<abbr>`)
//     // })
//     console.log(acronym)
//     console.log(fullText)
//   })
// }

// browser.runtime.onMessage.addListener(loafifyPage)